function main() {
    // const nodes = Array.from(document.querySelectorAll("[fixed-height]"));
    // nodes.forEach(i => {
    //     i.style.height = i.offsetHeight + "px";
    //     // console.log(i.style.height);
    // });

    // window.onresize = () => {
    //     console.log("resize")

    //     nodes.forEach(i => {
    //         i.offsetHeight;
    //         i.style.height = "auto";
    //         i.offsetHeight;

    //         i.style.height = i.offsetHeight + "px";
    //     });
    // }

}

main();